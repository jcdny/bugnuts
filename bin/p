#!/usr/bin/env sh
ROOT=~/src/ai
EXE=$ROOT/bot/bin/bot.sh
TOOLS=$ROOT/tools
MAPS=$ROOT/bot/pkg/maps/testdata/maps
ARCH="`uname -s`"

export MAP1="random_walk_04p_02.map" # translation  symmetry
export MAP2="maze_04p_02.map" # annoying map
export MAP3="maze_04p_01.map" # straight across from another hill.
export MAP4="mmaze_04p_01.map" # 6 hills per player - insane.
export MAP5="random_walk_04p_01.map" # empty
export MAP6="mmaze_04p_02.map"  # 2 hills each

export MAP="$MAPS/$MAP1"
echo $MAP

#    --nolaunch \

$TOOLS/playgame.py \
    --fill \
    -I -E -e -O -R \
    --player_seed 41 \
    --engine_seed 41 \
    --end_wait=0.25 \
    --verbose \
    --log_dir $ROOT/bot/log \
    --turns 400 \
    --map_file $MAP \
    --food_rate 5 11 \
    --food_turn 19 37 \
    --food_start 75 175 \
    --food_visible 3 5 \
    --cutoff_turn 150 \
    --cutoff_percent 0.85 \
    "$@" \
    "$EXE" \
    "$ROOT/bot/bin/$ARCH/bugnutsv5" \
    "python $TOOLS/sample_bots/python/GreedyBot.py" "python $TOOLS/sample_bots/python/HunterBot.py" \




